post_install() {
    if [ ! -e /usr/share/{pkgname}/config.php ]
    then
	cp -n /usr/share/doc/zarafa-webapp/config.php.dist /etc/webapps/zarafa-webapp/config.php
	cp -n /usr/share/doc/zarafa-webapp/debug.php.dist /etc/webapps/zarafa-webapp/debug.php
    fi
}